.modal.fade{id: modal_id, role: 'dialog', tabindex: '-1'}
  .modal-dialog{:role => 'document'}
    .modal-content
      .modal-header
        %button.close{'aria-label' => 'Close', 'data-dismiss' => 'modal', :type => 'button'}
          %span{'aria-hidden' => 'true'} ×
        %h4.modal-title= "Is this a real #{object_name}?"
      .modal-body
        %p= "The vast majority of anonymous #{object_name.pluralize} are legitimate."
        %br
        %p But sometimes people are just testing out the feature.
        %br
        %p= "If this is a fake #{object_name}, please cancel the action below."
        %br
        %p= "But if this is a real #{object_name} then post away. And Thank You!"

      .modal-footer
        %span.btn.btn-default{data: {dismiss: 'modal'}}= "Cancel: not a real #{object_name}"
        = form.submit "Yes, it is a real #{object_name}", class: 'btn btn-action'
