<%= form_for(temporary_user) do |f| %>
  <% if temporary_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(temporary_user.errors.count, "error") %> prohibited this temporary_user from being saved:</h2>

      <ul>
      <% temporary_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip_address %>
    <%= f.text_field :ip_address %>
  </div>

  <div class="field">
    <%= f.label :votes %>
    <%= f.text_area :votes %>
  </div>

  <div class="field">
    <%= f.label :questions %>
    <%= f.text_area :questions %>
  </div>

  <div class="field">
    <%= f.label :answers %>
    <%= f.text_area :answers %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
