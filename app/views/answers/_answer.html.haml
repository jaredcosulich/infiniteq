-# .question.col-sm-12
-#   .vote.col-sm-1
-#     = render 'votes/vote' locals: {object: question, positive: true})
-#     .down-vote
-#       = render 'votes/vote' locals: {object: question, positive: false})
-#     .vote-total
-#       %h3
-#         .small= (question.vote_total.to_f / 10.0)
-#   .col-sm-10
-#     %h4
-#       %span.title Q:
-#       = link_to(question.text, question)

.answer.col-sm-12
  .vote.col-sm-1
    =# render 'votes/vote', locals: {object: question}
  .text= markdown(answer.text)
  .details.small
    = answer.created_at.strftime('%b %d, %y %-I:%M%p')
    &nbsp; &nbsp;
    = link_to fa_icon('pencil-square-o'), edit_answer_path(answer)
